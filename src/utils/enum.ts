export enum gameModeEnum {
  WORDS = "words",
  TIME = "time",
}

export enum WordsHistoryEnum {
  CORRECT = 0,
  INCORRECT = 1,
  EXTRA = 2,
  MISSED = 3,
}

export enum endingActions {
  NEXT = "next",
  RESTART = "restart",
}
